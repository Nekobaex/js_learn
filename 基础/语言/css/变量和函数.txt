变量定义:
  --key: value

函数
  // 若 --key 变量不存在, 则使用 下一个参数
  var(--key, alt)

  // 使用标签属性的值
  attr(herf)

  // 加载 图片, 文字, 字体
  url()

  // 计算数学表达式
  calc()

  min()
  max()
  clamp(min, value, max)

  // 重复数值 (专用于 grid-template-)
  repeat(3, 33.33%)
  repeat(3, 1px 2px 3px)
  repeat(auto-fill, 100px)